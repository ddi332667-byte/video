# Video

一个从零开始的视频项目仓库。

## 目录

- `assets/`: 放图片、音频、视频等原始素材
- `scripts/`: 放生成视频、处理素材、整理文案的脚本
- `docs/`: 放策划、分镜、文案和说明
- `outputs/`: 放导出结果

## 下一步

1. 把素材放入 `assets/`
2. 在 `docs/` 编写视频文案或分镜
3. 在 `scripts/` 添加生成视频的脚本
4. 把最终视频导出到 `outputs/`
